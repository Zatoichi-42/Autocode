# /plan — Think Before Building

effort: high

Load the `self-plan` skill. Run the 5-round self-interview protocol.
Produce .claude/PLAN.md with acceptance criteria, critique mitigations,
implementation sequence, and deferred items.

Use before any feature with >2 files of changes.
